<header class="site-header">
  <div class="container nav-shell">
    <a class="brand" href="/">DataBuilderz</a>
    <button class="nav-toggle" id="nav-toggle" aria-label="Toggle navigation" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
    <nav class="site-nav" id="site-nav" aria-label="Primary">
      <a <% if (activePage === 'home') { %>class="active"<% } %> href="/">Home</a>
      <a <% if (activePage === 'about') { %>class="active"<% } %> href="/about">About Us</a>
      <a <% if (activePage === 'services') { %>class="active"<% } %> href="/services">Services</a>
      <a <% if (activePage === 'case-studies') { %>class="active"<% } %> href="/case-studies">Case Studies</a>
      <a <% if (activePage === 'contact') { %>class="active"<% } %> href="/contact">Contact</a>
    </nav>
  </div>
</header>
